{
  "name": "app",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev:server": "bun --watch ./index.ts",
    "dev:styles": "npx tailwindcss -i ./index.css -o ./dist/styles.css --watch",
    "dev": "bun concurrently 'bun run dev:styles' 'bun run dev:server' --kill-others"
  }
}