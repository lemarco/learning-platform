// "use client";

// // import { GoogleOAuthProvider } from "@react-oauth/google";
// import { isGithubAuthEnabled, githubClientId } from "./github-config";

// export function GithubAuthProvider({ children }: { children: React.ReactNode }) {
//   return isGithubAuthEnabled && githubClientId ? <GoogleOAuthProvider clientId={githubClientId}>{children}</GoogleOAuthProvider> : children;
// }
